// bitacoras.service.js   ?? DEV 3 | Bitacoras / Evidencias
const prisma = require('../../core/config/db');

const getAll = async () => {
  // TODO: Implementar lógica del módulo bitacoras
  return [];
};

module.exports = { getAll };