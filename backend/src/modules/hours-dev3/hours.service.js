// hours.service.js   ?? DEV 3 | Contabilidad de horas
const prisma = require('../../core/config/db');

const getAll = async () => {
  // TODO: Implementar lógica del módulo hours
  return [];
};

module.exports = { getAll };