// novelties.service.js   ?? DEV 3 | Novedades / Incidentes
const prisma = require('../../core/config/db');

const getAll = async () => {
  // TODO: Implementar lógica del módulo novelties
  return [];
};

module.exports = { getAll };